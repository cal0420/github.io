const DISCORD_INVITE_URL="https://discord.gg/c4ghub";document.querySelectorAll("[data-discord]").forEach(a=>{a.href=DISCORD_INVITE_URL;a.target="_blank";a.rel="noopener noreferrer"});document.querySelectorAll("[data-year]").forEach(e=>e.textContent=new Date().getFullYear());const nav=document.querySelector("[data-nav]");const onScroll=()=>nav?.classList.toggle("scrolled",scrollY>20);onScroll();addEventListener("scroll",onScroll,{passive:true});const toggle=document.querySelector(".menu-toggle"),links=document.querySelector(".nav-links");toggle?.addEventListener("click",()=>{const open=links.classList.toggle("open");toggle.setAttribute("aria-expanded",open)});const file=location.pathname.split("/").pop()||"index.html";document.querySelectorAll(".nav-links a").forEach(a=>{if(a.getAttribute("href")===file)a.classList.add("active")});const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll(".reveal").forEach(e=>io.observe(e));document.querySelectorAll("[data-count]").forEach(el=>{let done=false;const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting&&!done){done=true;const target=+el.dataset.count;let n=0;const timer=setInterval(()=>{n++;el.textContent=n;if(n>=target)clearInterval(timer)},180);obs.disconnect()}}));obs.observe(el)});const search=document.querySelector("#commandSearch"),cards=[...document.querySelectorAll(".command-card")],empty=document.querySelector(".empty-state"),filterBtns=[...document.querySelectorAll("[data-filter]")];let category="all";function filterCommands(){const q=(search?.value||"").toLowerCase().trim();let visible=0;cards.forEach(card=>{const ok=(category==="all"||card.dataset.category===category)&&card.dataset.search.toLowerCase().includes(q);card.hidden=!ok;if(ok)visible++});if(empty)empty.hidden=visible!==0}search?.addEventListener("input",filterCommands);filterBtns.forEach(btn=>btn.addEventListener("click",()=>{filterBtns.forEach(b=>b.classList.remove("active"));btn.classList.add("active");category=btn.dataset.filter;filterCommands()}));const canvas=document.querySelector("#particles");if(canvas){const ctx=canvas.getContext("2d");let pts=[];function size(){canvas.width=innerWidth*devicePixelRatio;canvas.height=innerHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);pts=Array.from({length:Math.min(70,Math.floor(innerWidth/18))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.3+.2,v:Math.random()*.18+.04}))}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);ctx.fillStyle="rgba(218,177,255,.65)";pts.forEach(p=>{p.y-=p.v;if(p.y<0)p.y=innerHeight;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()});requestAnimationFrame(draw)}addEventListener("resize",size);size();draw()}